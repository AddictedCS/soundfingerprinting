<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
	<TargetFramework>netstandard2.0</TargetFramework>
	<AllowUnsafeBlocks>true</AllowUnsafeBlocks>
	<Nullable>enable</Nullable>
	<Authors>Sergiu Ciumac</Authors>
	<Company>Sergiu Ciumac</Company>
	<Product>SoundFingerprinting</Product>
	<Copyright>Copyright © Sergiu Ciumac 2026</Copyright>
	<Description>SoundFingerprinting is a C# framework that implements an efficient algorithm of audio fingerprinting and identification. Designed for developers, enthusiasts, researchers in the fields of audio processing, data mining, digital signal processing.</Description>
	<PackageProjectUrl>https://github.com/addictedcs/soundfingerprinting</PackageProjectUrl>
	<RepositoryUrl>https://github.com/AddictedCS/soundfingerprinting</RepositoryUrl>
	<RepositoryType>git</RepositoryType>
	<PackageReleaseNotes>
		Version 15.13.0
		- Hashes.MergeWith applies the earlier operand's TimeOffset once and accepts operands in any order, so folding realtime chunks no longer moves the window start back or stretches the duration
		Version 15.12.0
		- Version bump to 15.12.0
		Version 15.11.1
		- Realtime queries no longer lose accumulated coverage when a chunk re-matches the head of the track it already covered.
		Version 15.11.0
		- UIntModelReferenceTracker takes a wrapOnOverflow flag that restarts both counters at 0 instead of throwing, keeping every track in one contiguous block across the wrap, and reports the lap count and both write heads as one atomic snapshot through State
		- UIntModelReferenceProvider.TryNext issues the next reference without throwing and leaves the counter untouched when the maximum is reached
		- Adding AVHashes.GetRange that cuts a range out of both media types on absolute time
		Version 15.10.0
		- Adding playback-speed compensation for audio queries
		Version 15.9.1
		- Guard Origins and StreamId against protobuf null on deserialization
		Version 15.9.0
		- Realtime result entry concatenation applies negative query offsets (chunk overlap) to `QuerySequenceNumber` in signed arithmetic. Previously the offset went through a negative double to uint cast (saturates to 0 on .NET Core, unspecified on .NET Framework), leaving stitched sequence numbers ahead of their match times by the overlap length.
		- `Coverage.Confidence` is capped at 1. A matched path overhanging the declared query/track length (e.g. the last fingerprint of a full-length match extending past the stored track duration) shrank the denominator below the numerator, yielding confidence above 1.
		- Fixed an out-of-bounds spectrum read: `UseDynamicLogBase = false` with `FrequencyRange.Max` at or near Nyquist produced a spectrum bin index one past the last stored bin, silently folding adjacent stack memory into fingerprints. The index now scales by N/2 (was N/2 + 1) and clamps at the last bin. Static log base bin boundaries above roughly Nyquist/2 shift down by one spectral bin, so fingerprints generated with static log base differ marginally from prior versions; the default dynamic log base configuration is unaffected.
		- Realtime query cancellation reaches sources supplied via `From(IAsyncEnumerable)` overloads (the token was silently discarded before the source enumerator; cancelling while awaiting the next chunk hung until the producer completed).
		- Realtime error handling: exceptions thrown by user callbacks no longer reroute already consumed hashes to `IOfflineStorage` (previously re-emitting the same results and double-counting query length); `ErrorCallback` is invoked after backoff registration and offline persistence, so the default rethrowing callback no longer loses hashes; cancellation during the error backoff delay delivers pending aggregated entries; the realtime samples aggregator survives source re-enumeration after transient errors, preserving the buffered chunk tail. Realtime sources must be hot (resume where they left off when re-enumerated) - documented on `IRealtimeSource`.
		- Performance: `Coverage` caches gap computations (previously recomputed on every property access, multiple times per candidate per query); `ExtractLogBins` accumulates raw magnitudes and scales once per bin; `TinyFingerprintSchema` is backed by ulong words with popcount (56x faster `TrueCounts`, 32x faster `AgreeOn`, 1.24x faster encoding, identical results).
		- Versioning: the release version is now a single `Version` property in src/Directory.Build.props from which package and assembly versions derive; assembly attributes are SDK-generated, so `AssemblyVersion` follows the package version exactly (the historical .100 revision suffix is gone) and `InformationalVersion` carries the source revision. New bump.sh automates version bumps and release notes.
		Version 15.8.0
		- Realtime `MatchedAt` timestamps no longer land late by the chunk-overlap prefix. Realtime aggregation prepends the tail of the previous chunk (~one fingerprint length) for FFT continuity across chunk boundaries and records the prefix in `Hashes.TimeOffset`, but the offset was never applied when deriving the absolute match time — every realtime match spanning aggregated chunks was stamped late by the prefix length (~1.8s with default configurations). Match time is now derived as `RelativeTo + TimeOffset + QueryMatchStartsAt`.
		- `Hashes.WithRelativeTo` preserves `TimeOffset` (previously reset it to 0, silently destroying the prefix record when realtime chunks were re-stamped with their capture time — the root cause of the above on file-fed realtime pipelines, and of downstream consumers double-counting the overlap on the query axis).
		- `RelativeTo` is uniformly the caller's content stamp and is never amended internally: `RealtimeAudioSamplesAggregator` no longer back-shifts the aggregated chunk's `RelativeTo` by the prefix length; the prefix is carried exclusively in `TimeOffset` and applied at every calculation deriving absolute time (`EndsAt`, `GetRange`, `MergeWith` alignment, match-time derivation). Consumers reading `RelativeTo` of realtime-aggregated chunks now observe the chunk's own stamp; the fingerprinted window starts at `RelativeTo + TimeOffset`.
		- Producers cutting overlapping chunks should stamp `Hashes.TimeOffset` with the negative overlap length; a non-zero producer-stamped offset is authoritative.
		Version 15.7.0
		- New `RealtimeQueryConfiguration.OngoingEntryStalenessCeiling` (default `double.MaxValue` — no behavior change unless set): caps the longest non-matching stretch, in seconds, an ongoing realtime result entry may bridge while waiting for the matched track to resume. Historically an ongoing entry was held until the query stream advanced past the track's remaining length, so a sporadic few-seconds match against a very long track stayed pending (accumulating query hashes) for the track's remaining duration, delaying downstream consumers that wait on entry completion. With a capped ceiling such entries complete shortly after they stop extending; entries whose coverage keeps growing are unaffected, and entries on tracks shorter than the ceiling are unaffected regardless of the value.
		Version 15.6.0
		- Adjusting Insert method signature.
		Version 15.5.1
		- Fixed head/tail bridges being refused when the boundary reals share a `QuerySequenceNumber` (a duplicate query-position match — one query sub-fingerprint matching several track sub-fingerprints). The endpoint slot period was derived from only the adjacent boundary pair, so `qSeqSpan = 0` yielded slope 0 and the whole leading/trailing edge was left unbridged (a genuine duplicate then wrongly reported not-full). The slope now anchors on the endpoint real and scans inward to the nearest real with a strictly distinct query sequence number.
		- `CompositeBridgingStrategy` now derives `MaxQueryRelativeBridge` / `MaxAbsoluteBridgeSeconds` as the maximum over its legs (was minimum), so composition is additive — adding a tighter leg (e.g. `SimilarProfileBridgingStrategy`) no longer starves the broadband/silence budget and drops interior + tail bridges.
		- `SimilarProfileBridgingStrategy` ΔSFM `tolerance` cap removed (was [0.05, 0.20], now (0, 1]); the caller controls bridging aggressiveness and is responsible for downstream false-merge safety.
		Version 15.5.0
		- Adjusting spectral profile bridging
		Version 15.4.0
		- Fixing synthetic candidates emissions
		Version 15.3.0
		- `FingerprintConfiguration.ComputeSpectralProfile` now defaults to `true`: tracks fingerprinted with the default config carry the per-second spectral profile, so enabling an `SfmMatchStrategy` at query time works out of the box instead of silently no-op'ing against profile-less tracks (the profile can only be computed while the original media is in hand). Opt out with `ComputeSpectralProfile = false` for storage-constrained catalogs. Match results are unchanged unless a bridging strategy is set; query configs are unaffected (the `SfmMatchStrategy` setter cascade keeps the flag off for `NoBridgingStrategy`).
		Version 15.2.0
		- `MatchedWith` now carries a `MatchedWithType` (`Fingerprint` | `Silence` | `BroadbandNoise` | `SimilarProfile`) recording whether a path entry is a real fingerprint hit or which spectral-bridging strategy synthesised it; bridging strategies stamp their synthetics and `Coverage.BridgedSeconds` is derived from it.
		- `TruePositivesFilter` gains `ignoreBridgedCoverage` (default `true`): when bridging is present the thresholds are re-evaluated against the fingerprint-only coverage, so a stepping-stone synthetic cannot inflate a weakly-anchored match over the bar (it would otherwise extend the span and tuck the gap under the permitted gap). No effect on coverages produced without bridging.
		Version 15.1.0
		- `CompositeBridgingStrategy` now returns a canonical (deduplicated, ascending-by-QueryMatchAt) union, making it independent of inner-strategy order; previously the concatenated runs could violate the path-reconstruction cursor invariant and drop head/tail bridges depending on leg order.
		- Removed `CompositeBridgingStrategy.BroadbandOrSilent` (breaking). Compose explicitly, e.g. `new CompositeBridgingStrategy(new BroadbandNoiseBridgingStrategy(), SilentRegionBridgingStrategy.Default)`.
		- `ISfmMatchStrategy` gains `MaxQueryRelativeBridge` and `MaxAbsoluteBridgeSeconds`; the Phase-2 budget is now per-strategy `min(absolute, relative × queryLength)`, replacing the hidden universal 70% cap (now the configurable default). `CompositeBridgingStrategy` derives both as the minimum over its legs.
		- `SimilarProfileBridgingStrategy`'s dual cap unified onto these properties (internal truncation removed); it is now composable — its tight cap travels via the min-combine.
		- `BroadbandNoiseBridgingStrategy` agreement is now a windowed mean of |ΔSFM| over `AgreementWindowRadius` seconds (presence stays pointwise). Defaults changed to `SfmThreshold = 0.40`, `AgreementWindowRadius = 2` (window makes the lower floor safe); `sfmThreshold` range widened to [0.20, 0.90]. `AgreementWindowRadius = 0` reproduces the prior pointwise gate.
		Version 15.0.9
		- Fixing Hashes.GetRange spectral profile slicing.
		Version 15.0.1
		- Fixing diagonal-distance swap that breaks track monotonicity.
		Version 15.0.0
		- SFM-based path bridging for second-tier ad-deduplication matching. Per-second spectral profile (SFM + power) captured during fingerprinting and used at path-reconstruction time to bridge regions where hashes don't match across recordings (e.g., Corona ad's broadband intro waves, Apple ad's silent brand-reveal moments).
		- New `FingerprintConfiguration.ComputeSpectralProfile` flag (default false). When set, per-Frame SFM/power is computed pre-normalization and attached to `Hashes.Properties` as a base64-encoded payload via `SpectralProfileCodecV1` (1 version byte + 1 reserved + interleaved sfm/power bytes per second).
		- New `QueryConfiguration.SfmMatchStrategy` property (default `NoBridgingStrategy`). Setter cascades to `FingerprintConfiguration.ComputeSpectralProfile` so the query-side flag stays in lock-step with the chosen strategy.
		- New `ISfmMatchStrategy` interface with built-in strategies: `NoBridgingStrategy`, `BroadbandNoiseBridgingStrategy` (per-second SFM &gt; 0.70 on both sides + |Δ| &lt; 0.15), `SilentRegionBridgingStrategy` (per-second power &lt; 5% on both sides + |Δ| &lt; 5%), `SimilarProfileBridgingStrategy` (|Δsfm| &lt; 0.10, capped at min(10s, 0.30 × queryLength)).
		- New `CompositeBridgingStrategy` for safe union of bridging strategies; ships with `CompositeBridgingStrategy.BroadbandOrSilent` static covering both broadband and silent regions in one pass. Rejects `SimilarProfileBridgingStrategy` (its own per-strategy cap is load-bearing) and `NoBridgingStrategy` (empty bridge) at construction.
		- `MatchedWithExtensions.GetCoverages` extended with two-phase synthetic emission: Phase 1 per-strategy per-second candidates; Phase 2 skip-if-real, local bracket-interpolation for sequence numbers, universal 70% cumulative cap. Synthetics are structurally indistinguishable from real matches downstream — no `IsSynthetic` field, no wire-format change to `MatchedWith`.
		- `LCS.Coverage.BridgedSeconds` added as `[ProtoMember(6)]` — wire-visible cumulative count of seconds bridged by the active strategy.
		- New `SpectralProfileExtensions`: `TrackInfo.WithMetaFieldsFromHashes(Hashes)` (Insert-side auto-propagation), `Hashes.WithPropertiesFromTrack(TrackInfo)` and `Hashes.WithPropertiesFromMetaFields(IDictionary)` (Read-side auto-propagation). `InMemoryModelService.Insert` and `ReadHashesByTrackId` call these so consumers don't have to hand-plumb the profile between `Hashes.Properties` and `TrackInfo.MetaFields`.
		- `Hashes.Merge` propagates the spectral profile across contiguous splices (≤0.5s gap); one-sided profiles are dropped (cannot fabricate the missing half).
		- Removed `FingerprintConfiguration.TreatSilenceAsSignal` (breaking change). Silent regions are now bridged at the path layer via `SilentRegionBridgingStrategy` — symmetric (both sides must be silent at the same second), capped by the universal 70% bound, power-based (handles fade-outs and near-silence, not just hard zero). Consumers with `TreatSilenceAsSignal = true` should drop the line and configure `SilentRegionBridgingStrategy.Default` (or `CompositeBridgingStrategy.BroadbandOrSilent`) on the query side.
		Version 14.1.0
		- Adding maxTotalDelay to RandomExponentialBackoffPolicy
		Version 14.0.0
		- Fixing parallelisim inside FingerprintService
		- QueryPathReconstructionStrategy lookup optimization O(1) instead of O(n^2)
		- Improving HashConverter performance by using direct bit manipulation instead of BitConverter
		Version 13.0.0
		- Upgrading .NET dependencies to .NET 10
		Version 12.8.0
		- Adding TruePositives filter with optional parameters.
		Version 12.6.0
		- Adding the ability to filter false positives results on configuration level (default is off).
		Version 12.5.0
		- Adding default black frames configuration
		Version 12.4.0
		- Model reference tracker has to generate contiguous sub-fingerprint ids for the same set of hashes.
		Version 12.1.0
		- Upgrading .NET dependencies to .NET 9.0.0
		Version 12.0.0
		- Accomodating SoundFingerprinting.Emy upgrade to FFmpeg 7.x
		Version 11.4.0
		- Passing stream identifier to ongoing result callback.
		Version 11.3.0
		- Adding IsFullMatch property to ResultEntry to indicate if the match is a duplicate match or not.
		Version 11.1.0
		- Adjusting `IRealtimeResultEntryFilter` implementation classes, making sure we adhere to the same principles across both configuration options `ResultEntryFilter` and `OngoingResultEntryFilter`.
		- Adding `ChainedRealtimeEntryFilter` which replaces `OngoingRealtimeResultEntryFilter`.
		- `TrackMatchLengthEntryFilter` can be used as `TrackCoverageLengthEntryFilter(waitTillCompletion: false)`
		- `OngoingRealtimeResultEntryFilter(x, y)` can be chained as new ChainedRealtimeEntryFilter([new TrackRelativeCoverageEntryFilter(x, waitTillCompletion: false), new TrackCoverageLengthEntryFilter(y, waitTillCompletion: false)`])
		- `TrackRelativeCoverageLengthEntryFilter` can be used as `TrackRelativeCoverageEntryFilter(waitTillCompletion: true)`.
		Version 11.0.0
		- Cleanup of the codebase, removing obsolete methods and classes.
		- Removed IAdvancedModelService.
		- Removed obsolete Query methods from IQueryService, leaving only QueryEfficient method.
		- Removed MediaType from QueryConfiguration, as it was not used anymore.
		Version 10.4.0
		- Added new property MetaFields to AVQueryMatch, to be able to store additional metadata about the match.
		Version 10.3.0
		- Improved the ability to reconstruct coverage from tone signal matches (silence can be treated as a tone signal).
		- Added a fingerprinting flag that allows including silence fingerprints in the generated result set.
		Version 10
		- Accomodating SoundFingerprinting.Emy upgrade to FFmpeg 6.x
		Version 9.5.0
		- Dependency upgrade to .NET 8
		Version 9.4.2
		- Contains an important improvement to QueryCommand, allowing a much faster lookup for the use-cases when the track and the query are very long and almost identical.
		- The improvement is rooted in the idea of returning Candidates from the IModelService instead of list of SubFingerprints. This provided knowledge on which hashed fingerprint matched with the query.
		- To further improve the API design, methods related to the correct functioning of the QueryCommand were extracted into IQueryService interface (from which IModelService derives).
	</PackageReleaseNotes>
	<PackageTags>Audio Video Identification Fingerprinting Digital Signal Processing Music Recognition Data Mining Content Sound Shazam</PackageTags>
	<LangVersion>latest</LangVersion>
    <GenerateDocumentationFile>true</GenerateDocumentationFile>
	<PackageLicenseFile>license.txt</PackageLicenseFile>
    <PackageIcon>soundfingerprinting.png</PackageIcon>
    <PackageReadmeFile>README.md</PackageReadmeFile>  
  </PropertyGroup>
	<PropertyGroup>
		<CodeAnalysisRuleSet>../Emy.ruleset</CodeAnalysisRuleSet>
	</PropertyGroup>
  <Target Name="PrepublishScript" BeforeTargets="PrepareForPublish">
	<ItemGroup>
		<DocFile Include="bin\$(Configuration)\$(TargetFramework)\*.xml" />
	</ItemGroup>
	<Copy SourceFiles="@(DocFile)" DestinationFolder="$(PublishDir)" SkipUnchangedFiles="false" />
  </Target>
  <ItemGroup>
	<PackageReference Include="StyleCop.Analyzers" Version="1.1.118">
		<PrivateAssets>all</PrivateAssets>
		<IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
	</PackageReference>
	<AdditionalFiles Include="../stylecop.json" />
  </ItemGroup>
  <ItemGroup>
	  <PackageReference Include="Microsoft.Extensions.Logging.Abstractions" Version="10.0.0" />
	  <PackageReference Include="Microsoft.Bcl.AsyncInterfaces" Version="10.0.0" />
	  <PackageReference Include="Microsoft.CodeAnalysis.NetAnalyzers" Version="10.0.100">
		  <PrivateAssets>all</PrivateAssets>
		  <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
	  </PackageReference>
	  <PackageReference Include="protobuf-net" Version="2.4.6" />
  </ItemGroup>
  <ItemGroup>
	<None Include="../../license.txt">
		<Pack>True</Pack>
		<PackagePath></PackagePath>
	</None>
    <None Include="../../soundfingerprinting.png">
	   <Pack>True</Pack>
	   <PackagePath></PackagePath>
	</None>
	<None Include="../../README.md">
	   <Pack>True</Pack>
	   <PackagePath></PackagePath>
	</None>
  </ItemGroup>
</Project>
